{
    "version": 2,
    "buildCommand": "echo 'No build needed - static files'",
    "outputDirectory": "landing-page",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}